import type { Assessment } from '../../../types/assessment';

export async function listAssessments(filters: any): Promise<Assessment[]> {
  // TODO: wire to your backend
  console.warn('listAssessments stub called', filters);
  return [];
}

export async function listStudentAssessments(filters: any): Promise<any[]> {
  // TODO: wire to your backend
  console.warn('listStudentAssessments stub called', filters);
  return [];
}