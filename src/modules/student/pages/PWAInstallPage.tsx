// Item 4.5: one shared install page, pinned to the student copy.
import PWAInstallPage from '@/shared/pages/PWAInstallPage';

export default function StudentPWAInstallPage() {
  return <PWAInstallPage role="student" />;
}
