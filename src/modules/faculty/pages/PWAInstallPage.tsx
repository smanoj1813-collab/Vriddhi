// Item 4.5: one shared install page, pinned to the faculty copy.
import PWAInstallPage from '@/shared/pages/PWAInstallPage';

export default function FacultyPWAInstallPage() {
  return <PWAInstallPage role="faculty" />;
}
